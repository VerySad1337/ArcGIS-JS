server {
    listen 443 ssl;
    # The ArcGIS SDK ships as a very large number of small ES module chunks,
    # and Vite emits a <link rel="modulepreload"> for each one the entry
    # statically imports - a few hundred requests before the map can draw. On
    # HTTP/1.1 a browser opens at most ~6 connections per origin, so those
    # requests queue in waves and the waterfall, not the bytes, becomes the
    # bottleneck. HTTP/2 multiplexes them over one connection. Directive form
    # (rather than `listen 443 ssl http2`) per nginx 1.25.1+.
    http2 on;
    server_name _;
    root /usr/share/nginx/html;
    index index.html;

    # Self-signed, generated at image build time (see Dockerfile) - fine for
    # local/dev use behind localhost:8080, but browsers will show an
    # untrusted-certificate warning since nothing signed it. Swap these two
    # paths for a real cert (e.g. from a reverse proxy/Let's Encrypt) for
    # anything beyond local dev.
    ssl_certificate /etc/nginx/ssl/localhost.crt;
    ssl_certificate_key /etc/nginx/ssl/localhost.key;

    # The stock nginx image ships with gzip commented out, so without this
    # every asset went over the wire uncompressed - the entry chunk alone is
    # ~3.7 MB raw against ~1.0 MB gzipped, i.e. the single largest cost on a
    # cold load, ahead of anything in the application code. JS/CSS/JSON/SVG
    # are all highly compressible; the .woff2 fonts and the ArcGIS .wasm/
    # image assets are already compressed formats and are deliberately left
    # out (re-compressing them burns CPU for no gain).
    gzip on;
    gzip_comp_level 6;
    # Below ~1 KB the header overhead outweighs the saving.
    gzip_min_length 1024;
    # Caches (and the immutable /assets/ policy below) must key on encoding,
    # or a proxy can hand a gzipped body to a client that never asked for one.
    gzip_vary on;
    gzip_types
        application/javascript
        text/javascript
        text/css
        application/json
        image/svg+xml
        text/plain;

    # Vite content-hashes every file under /assets on each build (e.g.
    # I3SIndexInfo-CpQp5Naa.js), so a given filename's content never changes -
    # safe to cache for a year. This is also what most of the app's 3D-only
    # (SceneView) chunks are: they're dynamically imported by @arcgis/core
    # only the first time a user switches to 3D, not on initial page load.
    location /assets/ {
        add_header Cache-Control "public, max-age=31536000, immutable";
    }

    # index.html is NOT content-hashed and is the one file whose byte content
    # legitimately changes on every deploy (it references that deploy's own
    # asset hashes). If it's cached, a browser tab left open across a
    # redeploy keeps running the old build's JS while nginx now serves a
    # dist/ with different asset hashes - any code path that only fetches a
    # chunk lazily (like the 2D/3D toggle's SceneView modules) then 404s the
    # moment it's finally exercised, well after the redeploy already
    # succeeded. no-cache forces a revalidation on every load instead.
    location = /index.html {
        add_header Cache-Control "no-cache";
    }

    # Regenerated by docker-entrypoint.sh on every container start from the
    # runtime environment (VITE_ARCGIS_*) - must always revalidate, same
    # reasoning as index.html above, so a browser never holds onto a stale
    # config from a previous container/deploy.
    location = /env-config.js {
        add_header Cache-Control "no-cache";
    }

    # Reverse-proxies to the onemap-proxy service (see docker-compose.yml)
    # so the frontend can call its own origin (same-origin, no CORS needed)
    # instead of talking to that service directly - the browser never gets
    # a hostname/port for onemap-proxy to reach on its own, since that
    # service isn't published outside the compose network at all.
    #
    # 127.0.0.11 is Docker Compose's embedded DNS resolver. Resolving
    # "onemap-proxy" through a `resolver` + variable (rather than a plain
    # `proxy_pass http://onemap-proxy:4000/`) makes nginx re-resolve it per
    # request instead of once at startup - a plain proxy_pass would fail
    # nginx's own startup if the onemap-proxy container weren't already up
    # and DNS-resolvable at that exact moment, which compose does not
    # guarantee even with depends_on (that only orders container start, not
    # "app inside is ready").
    location /api/onemap/ {
        resolver 127.0.0.11 valid=10s;
        set $onemap_upstream onemap-proxy:4000;
        # No path after $onemap_upstream (and no trailing slash) is
        # deliberate: with a variable in proxy_pass, nginx does NOT do its
        # usual "strip the matched location prefix" URI rewriting - the
        # original request URI (e.g. /api/onemap/token) is forwarded
        # unchanged, which matches server.js's routes exactly (it also
        # answers at /api/onemap/token, not /token).
        proxy_pass http://$onemap_upstream;
        proxy_set_header Host $host;
    }

    # Reverse-proxies to the mcp-chat-proxy service (see docker-compose.yml)
    # so the frontend can call its own origin for the chat endpoints - same
    # same-origin/no-CORS reasoning, and the same re-resolve-per-request
    # pattern, as the /api/onemap/ block above.
    location /api/chat/ {
        resolver 127.0.0.11 valid=10s;
        set $chat_upstream mcp-chat-proxy:4001;
        proxy_pass http://$chat_upstream;
        proxy_set_header Host $host;
        # A local-model chat turn (or a chain of tool calls) can run well
        # past nginx's default 60s proxy_read_timeout - extend it rather
        # than have a legitimately-slow model response come back as a
        # gateway timeout. Measured directly on CPU-only Ollama against
        # this app's real tool-schema payload: ~220s for one turn (see
        # mcp-chat-proxy/config.js's ollamaRequestTimeoutMs comment) - kept
        # above that service's own 300s timeout so mcp-chat-proxy's clear
        # error surfaces first instead of a bare nginx 504.
        proxy_read_timeout 340s;
    }

    location / {
        try_files $uri $uri/ /index.html;
    }
}
