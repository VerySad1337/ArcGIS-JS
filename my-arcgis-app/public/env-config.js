// Placeholder for local dev (`npm run dev` / `vite build` without Docker).
// In the container, docker-entrypoint.sh overwrites this file at startup
// with real values from the runtime environment. See ArcGISConfiguration.js.
window.__ENV__ = {};
